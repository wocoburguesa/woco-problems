#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <sstream>
#include <math.h>

using namespace std;

int main(){
  stringstream ss;
  while(getline(cin, ss) && ss != "Game Over")
  return 0;
}
